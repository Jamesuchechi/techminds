// Destructuring

const student = {
    name:"Ola",
    age:10,
    height:6.2
};
const {name, age} = student;

console.log(name);
console.log(age);

// Array

const colors = [ "red", "blue", "white"];
const [first, second] = colors;

console.log(first);